# --- QMIX specific parameters ---

# --- Checkpoint and Replay parameters ---
checkpoint_path: "results/models/sc2_8m_vs_9m-obs_aid=1-obs_act=0/algo=spe_qmix-agent=sper/env_n=8/rnn_dim=64-2bs=5000_128-tdlambda=0.6-epdec_0.05=100k/spe_qmix__2025-02-18_16-37-16" # Load a checkpoint from this path
evaluate: True # Evaluate model for test_nepisode episodes and quit (no training)
load_step: 0 # Load model trained on this many timesteps (0 if choose max possible)
save_replay: True # Save the replay of the model loaded from checkpoint_path
test_nepisode: 32 # Number of episodes to test for

# use epsilon greedy action selector
action_selector: "epsilon_greedy"
epsilon_start: 1.0
epsilon_finish: 0.05
epsilon_anneal_time: 100000 # 500000 for 6h_vs_8z

runner: "parallel"
batch_size_run: 1 # batch_size_run=4, buffer_size = 2500, batch_size=64  for 3s5z_vs_3s6z
buffer_size: 5000 
batch_size: 128

t_max: 10050000

# update the target network every {} episodes
target_update_interval: 200

# use the Q_Learner to train
mac: "spe_mac"
#agent: "hpn_rnn"
agent: "sper"

hpn_hyper_dim: 64
hpn_hyper_activation: 'elu'

hpn_head_num: 1 # 2 for 3s_vs_5z and 6h_vs_8z
agent_output_type: q

learner: "nq_learner"
mixer: "qmix"
mixing_embed_dim: 32
hypernet_embed: 64
lr: 0.001 # Learning rate for agents
td_lambda: 0.6 # 0.3 for 6h_vs_8z
optimizer: 'adam'
q_lambda: False



name: "spe_qmix"

obs_agent_id: True # Include the agent's one_hot id in the observation
obs_last_action: False # Include the agent's last action (one_hot) in the observation